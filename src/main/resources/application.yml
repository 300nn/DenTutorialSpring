spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/testBD
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
server:
  port: 8080

